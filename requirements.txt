/* static/style.css */

/* === GLOBAL & LIGHT THEME === */
body {
    font-family: 'Poppins', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    margin: 0;
    color: #fff;
    background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
    background-size: 400% 400%;
    animation: gradientShift 15s ease infinite;
    transition: background-color 0.5s ease;
    overflow-x: hidden;
}

@keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

/* === HEADER === */
.main-header { padding: 15px 40px; display: flex; justify-content: space-between; align-items: center; background: rgba(0,0,0,0.1); backdrop-filter: blur(10px); position: absolute; width: calc(100% - 80px); top: 0; left: 0; z-index: 10; }
.logo { font-size: 1.8em; font-weight: 700; }
.header-nav { display: flex; align-items: center; gap: 20px; }
.welcome-user { font-size: 0.9em; font-weight: 300; }
.theme-toggle-btn { background: none; border: 1px solid rgba(255,255,255,0.4); color: white; padding: 8px; border-radius: 50%; cursor: pointer; width: 40px; height: 40px; font-size: 1em; transition: background-color 0.3s ease; }
.theme-toggle-btn:hover { background-color: rgba(255,255,255,0.1); }
.icon-moon { display: none; }
.profile-link { font-size: 1.5em; text-decoration: none; color: #fff; transition: transform 0.2s ease; }
.profile-link:hover { transform: scale(1.1); }
.logout-link { color: #fff; text-decoration: none; font-weight: 600; background: rgba(211, 84, 0, 0.8); padding: 8px 15px; border-radius: 8px; transition: background-color 0.3s ease; }
.logout-link:hover { background: #d35400; }

/* === MAIN CONTENT (HOMEPAGE) === */
.main-container { display: flex; flex-direction: column; justify-content: center; align-items: center; min-height: 100vh; padding: 100px 20px 20px 20px; position: relative; z-index: 5; box-sizing: border-box; }
.title-section { text-align: center; }
.valued-phrase { font-size: 1.5em; font-weight: 600; text-transform: uppercase; letter-spacing: 3px; margin-bottom: 20px; animation: pulseGlow 4s infinite ease-in-out; }
@keyframes pulseGlow { 0%, 100% { text-shadow: 0 0 10px #fff, 0 0 20px #fff, 0 0 30px #e73c7e; } 50% { text-shadow: 0 0 20px #fff, 0 0 30px #ee7752, 0 0 40px #ee7752; } }
.title-section h1 { font-size: 3em; font-weight: 700; margin: 0; text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.6); }

/* === SEARCH WRAPPER & TABS === */
.search-wrapper { width: 100%; max-width: 700px; transition: transform 0.3s ease-out, box-shadow 0.3s ease-out; border-radius: 18px; margin-top: 40px; }
.search-wrapper:hover { transform: translateY(-12px); box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4); }
.search-tabs { display: flex; }
.tab-link { font-family: 'Poppins', sans-serif; padding: 10px 20px; border: none; background: rgba(0,0,0,0.2); color: #fff; cursor: pointer; border-radius: 10px 10px 0 0; font-size: 1em; font-weight: 600; transition: background-color 0.3s ease; }
.tab-link.active { background: rgba(0,0,0,0.4); }
.search-content { display: none; }
.search-content.active { display: block; }
.search-form { background: rgba(0,0,0,0.4); backdrop-filter: blur(12px); padding: 30px; border-radius: 0 18px 18px 18px; border: 1px solid rgba(255, 255, 255, 0.15); }
#keyword-search .search-form { display: flex; flex-wrap: wrap; gap: 15px; align-items: flex-end; }
.search-field { display: flex; flex-direction: column; gap: 8px; flex-grow: 1; min-width: 220px; }
.search-field label { font-weight: 600; margin-left: 10px; font-size: 0.9em; text-shadow: 1px 1px 2px rgba(0,0,0,0.4); }
.search-field input, .search-field textarea { border: none; background-color: rgba(255, 255, 255, 0.8); color: #333; padding: 14px 18px; border-radius: 8px; font-size: 1em; outline: none; transition: box-shadow 0.3s ease; font-family: 'Poppins', sans-serif; }
.search-field input::placeholder, .search-field textarea::placeholder { color: #555; }
.search-field input:focus, .search-field textarea:focus { box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.5); }
.search-button { border: none; background: #3498db; color: white; padding: 14px 35px; font-size: 1.1em; font-weight: 600; border-radius: 8px; cursor: pointer; transition: background-color 0.3s ease; height: 51px; display: flex; justify-content: center; align-items: center; }
.search-button:hover { background: #2980b9; }

/* === AI SEARCH STYLES === */
.ai-description { font-size: 0.9em; text-align: center; margin-top: 0; margin-bottom: 20px; opacity: 0.8; }
.descriptive-field { width: 100%; min-width: 100%; }
.search-field textarea { width: 100%; box-sizing: border-box; resize: vertical; }
.loader { border: 3px solid #f3f3f3; border-top: 3px solid #2980b9; border-radius: 50%; width: 24px; height: 24px; animation: spin 1s linear infinite; }
@keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }

/* === RESULTS PAGE & MASONRY LAYOUT === */
.results-page { justify-content: flex-start; }
.results-page .title-section p { font-size: 1.1em; margin: 0; }
.results-grid-wrapper { width: 100%; max-width: 1200px; margin-top: 30px; }
.results-container { column-count: 3; column-gap: 20px; }
.job-card { display: inline-block; width: 100%; margin-bottom: 20px; background: rgba(0, 0, 0, 0.2); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.15); border-radius: 18px; padding: 25px; transition: all 0.3s ease; box-sizing: border-box; }
.job-card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.2); }
.full-width-card { column-span: all; text-align: center; }
.back-link { color: #fff; text-decoration: none; background: rgba(0, 0, 0, 0.25); padding: 8px 15px; border-radius: 20px; transition: background-color 0.3s ease; display: inline-block; margin-top: 15px; }
.error-card { background: rgba(255, 107, 107, 0.2); border-color: rgba(255, 107, 107, 0.4); }

/* === PROFILE PAGE === */
.profile-box { width: 100%; max-width: 500px; padding: 40px; background: rgba(0, 0, 0, 0.4); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.15); border-radius: 18px; text-align: center; }
.profile-box h2 { margin-top: 0; margin-bottom: 30px; }
.profile-info p { font-size: 1.2em; }
.back-link-profile { color: #fff; text-decoration: none; background: rgba(0, 0, 0, 0.25); padding: 8px 15px; border-radius: 20px; transition: background-color 0.3s ease; display: inline-block; margin-top: 15px; }

/* === AUTH PAGES (LOGIN/SIGNUP) === */
.auth-container { display: flex; justify-content: center; align-items: center; min-height: 100vh; padding: 20px; }
.auth-box { width: 100%; max-width: 400px; padding: 40px; background: rgba(0, 0, 0, 0.4); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.15); border-radius: 18px; text-align: center; }
.auth-box h2 { margin-top: 0; margin-bottom: 30px; font-size: 2em; }
.input-group { text-align: left; margin-bottom: 20px; }
.input-group label { display: block; margin-bottom: 8px; font-weight: 600; }
.input-group input { width: 100%; padding: 12px 15px; border-radius: 8px; border: 1px solid transparent; background-color: rgba(255, 255, 255, 0.8); color: #333; font-size: 1em; box-sizing: border-box; }
.auth-button { width: 100%; padding: 12px; border: none; border-radius: 8px; background: #3498db; color: white; font-size: 1.1em; font-weight: 600; cursor: pointer; transition: background-color 0.3s ease; }
.auth-button:hover { background: #2980b9; }
.auth-switch { margin-top: 25px; font-size: 0.9em; }
.auth-switch a { color: #3498db; font-weight: 600; text-decoration: none; }
.flash { padding: 15px; margin-bottom: 20px; border-radius: 8px; text-align: center; font-weight: 600; }
.flash.error { background-color: rgba(255, 107, 107, 0.3); color: #fff; }
.flash.success { background-color: rgba(46, 204, 113, 0.3); color: #fff; }

/* === DARK THEME STYLES === */
body.dark-mode { background: #111827; animation: none; color: #d1d5db; }
body.dark-mode .valued-phrase { animation: pulseGlowDark 4s infinite ease-in-out; }
@keyframes pulseGlowDark { 0%, 100% { text-shadow: 0 0 10px #fff, 0 0 20px #3498db, 0 0 30px #3498db; } 50% { text-shadow: 0 0 20px #fff, 0 0 30px #23a6d5, 0 0 40px #23a6d5; } }
body.dark-mode .main-header, body.dark-mode .search-form, body.dark-mode .job-card, body.dark-mode .profile-box, body.dark-mode .auth-box { background: rgba(0,0,0,0.5); border-color: rgba(255,255,255,0.1); }
body.dark-mode .icon-sun { display: none; }
body.dark-mode .icon-moon { display: inline; }
/* Add other dark mode specifics as needed */

/* === PARTICLE ANIMATION === */
.particle-container { position: absolute; top: 0; left: 0; width: 100%; height: 100%; overflow: hidden; z-index: 1; }
.particle { position: absolute; bottom: -20px; background-color: rgba(255, 255, 255, 0.15); border-radius: 50%; animation: float 20s infinite ease-in; }
@keyframes float { from { transform: translateY(0) scale(1); opacity: 1; } to { transform: translateY(-100vh) scale(0); opacity: 0; } }
/* Add more particle rules for density... */
.particle:nth-child(1) { left: 5%; animation-duration: 15s; } .particle:nth-child(2) { left: 15%; animation-duration: 20s; }

/* === RESPONSIVE DESIGN === */
@media (max-width: 900px) { .results-container { column-count: 2; } }
@media (max-width: 768px) { .main-header { padding: 15px 20px; width: calc(100% - 40px); } .welcome-user { display: none; } .title-section h1 { font-size: 2.2em; } #keyword-search .search-form { flex-direction: column; align-items: stretch; } .search-button { height: auto; } }
@media (max-width: 600px) { .results-container { column-count: 1; } }
